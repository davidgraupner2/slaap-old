version: '3.8'
services:
  dev-db:
    image: 'postgres'
    restart: always
    environment:
      POSGRES_USER: postgres
      POSTGRES_DB: slaap
      POSTGRES_PASSWORD: sIrlk46Wlxa73jirI@rO
    logging:
      options:
        max-size: 10m
        max-file: '3'
    networks:
      - slaap-network
    ports:
      - '5434:5432'
    volumes:
      - ./postgres-data:/var/lib/postgres/data
      - ./src/database/providers/postgres/init_scripts/1.root.sql:/docker-entrypoint-initdb.d/1.root.sql
      - ./src/database/providers/postgres/init_scripts/2.root.data.sql:/docker-entrypoint-initdb.d/2.root.data.sql
networks:
  slaap-network:
